<Project>

  <!-- Target Frameworks -->
  <PropertyGroup>
    <TargetFrameworks Condition=" '$(MasteringProjectKind)' == 'module' ">net472;netstandard2.0;net6.0;net7.0</TargetFrameworks>
    <TargetFrameworks Condition=" '$(MasteringProjectKind)' == 'test' ">net472;net6.0;net7.0</TargetFrameworks>
  </PropertyGroup>

</Project>
